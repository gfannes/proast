#ifndef HEADER_proast_log_hpp_ALREADY_INCLUDED
#define HEADER_proast_log_hpp_ALREADY_INCLUDED

#include <ostream>

namespace proast { namespace log { 

    std::ostream &stream();

    std::string content();

} } 

#endif
